---
import urlFor from "@/sanity/lib/urlForImage";

const { src, alt, width, height, className } = Astro.props;
const image = urlFor(src);
---

<img
  src={image.width(width).height(height).url()}
  alt={alt}
  width={width}
  height={height}
  class={className}
  loading="eager"
/>
